package practices;

public class Car extends Vehicle {

	@Override
	public void getPrice() {
		double ratePerKM=0;
		double ratePerKMOnPetrol = StandardRates.ratePerKMPetrol.getRate();
		double ratePerKMOnDiesel = StandardRates.ratePerKMDiesel.getRate();
		double acCharge = StandardRates.AcCharge.getRate();
		if(Vehicle.)
	}

}
